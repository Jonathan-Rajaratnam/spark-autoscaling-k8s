apiVersion: batch/v1
kind: Job
metadata:
  name: spark-submit-hpa
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        strategy: hpa
        app: spark-submit
    spec:
      restartPolicy: Never
      containers:
        - name: spark-submit
          image: jonathan/spark-app:1.5
          imagePullPolicy: IfNotPresent
          command:
            - /opt/spark/bin/spark-submit
            - --master
            - spark://spark-master-hpa:7077
            - --deploy-mode
            - client
            - --conf
            - spark.driver.host=$(POD_IP)
            - --conf
            - spark.driver.bindAddress=0.0.0.0
            - --conf
            - spark.app.name=spark-app-hpa
            - --conf
            - spark.executor.memory=512m
            - --conf
            - spark.executor.cores=1
            - /opt/spark/app.py
            - property_prices  # Options: top_games, sentiment, user_activity, steam_heavy, property_prices
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: DATA_PATH
              value: /data
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: spark-data-pvc
