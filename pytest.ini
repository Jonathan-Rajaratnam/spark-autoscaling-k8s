[pytest]
# Java 17+ removed Subject.getSubject() used by Hadoop/PySpark 3.5.
# These flags must be available before the JVM starts, so we set them
# as an environment variable in pytest.ini rather than inside conftest.py.
filterwarnings =
    ignore::DeprecationWarning
